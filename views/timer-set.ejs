<!DOCTYPE html>
<html lang="en" dir="ltr">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Timer set</title>
  <script src="https://code.jquery.com/jquery-3.5.1.min.js" integrity="sha256-9/aliU8dGd2tb6OSsuzixeV4y/faTqgFtohetphbbj0=" crossorigin="anonymous"></script>
  <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
  <div class="timerSetContent">
    <h1 class="timer-text heading-timer">Set the Timer</h1>

    <form class="timer" action="/timer-set" method="post">
      <label class="timer-text">Minutes: </label>
      <select name="minutes" id="minutes">
        <% for (i = 0; i < 60;  i++ ) { %>
        <option value="<%= i %>" class="timer-text"> <%= i %> </option>;
        <% } %>
      </select>
      <label class="timer-text">Seconds:</label>
      <select name="seconds" id="seconds">
        <% for (i = 0; i < 60;  i++ ) { %>
        <option value="<%= i %>"><%= i %> </option>;
        <% } %>
        <input class="timerSetBtn" type="submit">
    </form>
  </div>
</body>
</html>
